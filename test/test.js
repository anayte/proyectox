var assert = require('assert');
describe('Array', function() {
  describe('#indexOf()', function() {
    it('Esta aplicación debería mostrar los datos descritos en el HTML', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});